---
import { Image } from 'astro:assets';
import heroImage from '../assets/images/hero-forest.png';
---

<section class="hero">
  <div class="hero-content">
    <h1>Legyetek még jobb társak a kutyáddal!</h1>
    <p>Kényszerítés, büntetés és bántalmazás mentes kutyaoktatás Szegeden és környékén</p>
    <a href="#kurzusok" class="cta-button">Kurzusok megtekintése</a>
  </div>
  <div class="hero-image">
    <Image
      src={heroImage}
      alt="Boldog kutya gazdájával"
      width={600}
      height={400}
      loading="eager"
    />
  </div>
</section>

<style>
  .hero {
    min-height: 80vh;
    display: flex;
    align-items: center;
    padding: 10rem 1.5rem 4rem;
    max-width: var(--max-width);
    margin: 0 auto;
    gap: 3rem;
  }

  .hero-content {
    flex: 1;
  }

  .hero h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    line-height: 1.2;
    color: var(--color-primary);
    margin-bottom: 1rem;
  }

  .hero p {
    font-size: 1.15rem;
    color: var(--color-text-light);
    margin-bottom: 2rem;
    max-width: 500px;
  }

  .cta-button {
    display: inline-block;
    background: var(--color-primary);
    color: white;
    padding: 1rem 2rem;
    border-radius: 4px;
    font-weight: 600;
    transition: background 0.2s, transform 0.2s;
  }

  .cta-button:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
  }

  .hero-image {
    flex: 1;
  }

  .hero-image :global(img) {
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    width: 100%;
    height: auto;
  }

  @media (max-width: 768px) {
    .hero {
      flex-direction: column;
      min-height: auto;
      padding-top: 10rem;
    }

    .hero-content {
      text-align: center;
    }

    .hero p {
      margin-left: auto;
      margin-right: auto;
    }
  }
</style>
